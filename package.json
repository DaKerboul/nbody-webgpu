{
  "name": "nbody-webgpu",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "An N-body galaxy integrated on your GPU, in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.52",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
